name: Integration Tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  integration-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: pip install -r requirements-test.txt

      - name: Install immudb-py for in-process tamper tests
        run: pip install immudb-py==1.5.0

      - name: Generate ImmuDB signing key pair
        run: make keygen

      - name: Run integration tests
        env:
          IMMUDB_USER: immudb
          IMMUDB_PASSWORD: immudb
          CONTROL_PLANE_READ_KEY: test-read-key
          CONTROL_PLANE_WRITE_KEY: test-write-key
          DASHBOARD_READ_USER: test-dashboard-reader
          DASHBOARD_READ_PASSWORD: test-dashboard-read-pw
          DASHBOARD_WRITE_USER: test-dashboard-writer
          DASHBOARD_WRITE_PASSWORD: test-dashboard-write-pw
        run: make test-integration
