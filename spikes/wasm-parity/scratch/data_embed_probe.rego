# SPIKE SCRATCH FILE - probes whether a bundle's data.json is compiled into
# the wasm binary itself, or must be supplied by the host at runtime via
# setData(). Not part of the AIL policy source tree.
package ail.probe2

baked_value := data.ail.probe2_data.some_constant
