# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/
# dashboard/lib/ contains TypeScript source (not a venv) — must be tracked
!dashboard/lib/

# Virtual environments
venv/
.venv/
env/
ENV/

# Environment variables
.env
.env.*
!.env.example

# IDE / AI tooling
.vscode/
.idea/
.claude/
*.swp
*.swo
.windsurfrules

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Jupyter
.ipynb_checkpoints/

# SQLite databases
*.db
*.sqlite
*.sqlite3

# ImmuDB signing key pair (generated by "make keygen"; never commit private key)
keys/signing.key
keys/signing.pub

# D14 (Phase 2) demonstration vault token - generated by "make keygen",
# delivered to the decision-service container only via a Compose secret.
# Never commit it, same reasoning as the signing key above.
decision_service/secrets/vault_api_token.txt
