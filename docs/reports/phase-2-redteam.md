# Phase 2 red-team report

**Run id:** `rt-p2-a`.

## 1. Run identity, environment, developer-state confirmation

**Working directory (this session):** `c:\Users\banji\OneDrive\Documents\compliance-ail` (primary), branch `main`, `HEAD` `6dd56b6`, `git status --short` empty throughout. This session performed no live attacks in the primary directory — all infrastructure work happened in a scratch clone. No branch anywhere in the repository carries the run id `rt-p2-a`.

**Base audited:** PR #8, branch `phase-2-boundary`. Resolved live via `gh pr view 8` (state `OPEN`, `mergeable`) and `git log origin/phase-2-boundary -1`: tip `4c6ff3abf86e90ad2fbded0ddf13b7a8aa48f0d9` ("docs(reports): record CI run id for phase-2.md"). **Audited: this live tip, `4c6ff3a`.**

**Scratch clone:** `rt-p2-a` (a directory name not used by any earlier session in this scratchpad — confirmed against `redteam-0.1`, `redteam-1`, `redteam-1-1`, `redteam-1-2`, `rt-p13-a`, `v8-clean-clone`). `git clone --branch phase-2-boundary` from the real GitHub remote. `docker compose -f docker-compose.yml -p rt-p2-a build --no-cache` for all five custom images (`ail-control-plane`, `decision-service`, `verifier`, `langgraph-demo`, `dashboard`) — every invocation carried the explicit compose project name `rt-p2-a`, per the brief. `make keygen`'s two artifacts (`keys/signing.key`/`.pub`, `decision_service/secrets/vault_api_token.txt`) generated by hand (`make` not installed on this host) using the exact commands the target reads from the Makefile. Full production stack (`docker-compose.yml`, real SPIRE/Envoy/decision-service) brought up with `up -d --wait`; all sixteen services healthy, including `langgraph-demo` completing its own SPIFFE handshake and reaching `decision-service` through Envoy (`Startup check: decision service reachable at https://envoy:8443/decide`).

**Concurrency discipline:** per the brief's own warning, no live attack ran while a pytest invocation was in flight against the same stack. The one full test-suite run against this stack (`tests/test_vault_tool_bypass.py`, §3 W4/W8) ran alone, after all manual probing for that claim had finished.

**Developer-state confirmation at the end:** `docker compose -f docker-compose.yml -p rt-p2-a down -v` — confirmed via `docker ps -a --filter name=rt-p2-a` → empty. All five built images removed (`docker rmi`), confirmed via `docker images | grep rt-p2-a` → empty. Scratch clone `git status --short` and `git diff --stat` both empty (every mutation made during W2/W3 testing — `docker-compose.yml`, `envoy.yaml` — was reverted from an in-memory backup and independently confirmed clean via git, not just recopied). Primary working directory: `git status --short` empty, `HEAD` unchanged at `6dd56b6`, branch unchanged (`main`).

---

## 2. Verdict table

| Claim | Verdict | One-line reason |
| :--- | :--- | :--- |
| W1 — no route to any control surface | **HOLDS** | Exhaustive live sweep (DNS, direct IP, host gateway, `host.docker.internal`, Envoy admin, shared-PID-namespace escalation, `docker.sock`, Envoy path-routing) found no reachable control surface; the one real finding (a shared PID namespace with Envoy/SPIRE-agent) yields process/socket-table visibility only, not code execution or a network route |
| W2 — boundary enforced against regression | **REFUTED** | Three of four named regressions are caught by a CI-equivalent run; the fourth — retargeting Envoy's cluster directly at a backend service — passes 29/29 of the no-stack-required test suite while live-reproducing a full OPA-management-API bypass, because no committed test ever inspects `envoy/envoy.yaml`'s content |
| W3 — `demonstrated` cannot be driven from config | **REFUTED** | `resolve_exclusivity_for` verifies by mechanism **string**, not by (tool, mechanism) pair — a tool registered after startup, or any second tool naming the same verified mechanism string, inherits `demonstrated` with zero independent verification of its own; live-confirmed against the real `schemas.py` module |
| W4 — vault credential unreachable from agent | **HOLDS** | `decision-service`/`vault_server.py` run in a private PID namespace never shared with the agent; schema and OPA-allowlist layers close every malformed/oversized/unapproved `secret_name` path before it can reach the vault; response messages are canned templates, never raw exception text; 8/9 committed `test_vault_tool_bypass.py` tests pass live (the 9th is a subprocess-timeout tooling artifact, not a security failure) |
| W5 — send-one-execute-another closed for mediated tools | **HOLDS** | No result reaches a caller whose call did not produce it (concurrent calls stay correctly paired); execution is strictly gated behind an ALLOW verdict; no replay/token mechanism exists |
| W6 — client leg has no fail-open, no double execution | **REFUTED** | Live-demonstrated: a genuine mediated-tool execution (a real vault-secret read, confirmed via decision-service's own MCP `CallToolRequest` log line) occurred with **zero** ledger record, because the ledger write is ordered strictly after execution with no compensating mechanism when the write subsequently fails — confirmed absent from ImmuDB by a direct raw scan |
| W7 — observed tools labelled honestly | **REFUTED** | The disclosed forgery path (README §5) is confirmed live — a record for `provision_cloud_server` (`observed`) can carry `profile: "mediated", exclusivity: "demonstrated"` — and, undisclosed, the dashboard renders this identically to a genuine record: no color, icon, or badge distinguishes it, unlike the rich iconography verification state gets |
| W8 — no prior attack reproduces, mapping is complete | **REFUTED** | U1/U5/U8/V2/V6 do not reproduce from either the agent's or the host's position (good) — but `docs/reports/phase-2.md`'s own §4 pre-registered negative ("every item has both live evidence and a test") is false on the report's own §3: the `envoy/envoy.yaml` mapping row cites only a live transcript, no test — exactly the gap W2 shows is live-exploitable and CI-invisible |

---

## 3. Evidence

### W1 — HOLDS

**Agent network position, confirmed:** `docker inspect rt-p2-a-langgraph-demo-1` → `rt-p2-a_edge: 172.22.0.4` only; `CapAdd: [] Privileged: false`; `PidMode: container:<spire-agent's container id>`. Backend services resolved to `rt-p2-a_backend` addresses (`172.21.0.0/24`), a disjoint subnet.

**DNS, direct IP, Envoy admin, `host.docker.internal`** — all from inside the agent container, real mTLS-attested position:
```
$ docker compose -p rt-p2-a exec -T -u ail-agent langgraph-demo python3 -c "socket.gethostbyname('opa')" [...5 more targets...]
opa: FAILED ([Errno -2] Name or service not known)   [same for verifier, ail-control-plane, immudb, decision-service, spire-server]

opa 172.21.0.9:8181: FAILED (timed out)               [same for verifier, control-plane, immudb x2, decision-service]

envoy-admin 172.22.0.2:9901: FAILED ([Errno 111] Connection refused)
envoy-admin 172.21.0.13:9901: FAILED (timed out)

host.docker.internal resolved: 192.168.65.254
  host.docker.internal:8181/8003/8002/8010/3322/8080: ALL FAILED ([Errno 111] Connection refused)
```
The `host.docker.internal` bypass that defeated Phase 1.3's loopback bind (`docs/reports/phase-1-3-redteam.md`, V2) does not apply here: `docker-compose.yml` publishes **no host port at all** for `opa`/`verifier`/`ail-control-plane`/`immudb`/`decision-service` (confirmed by reading the file; `ports:` is absent from all five service blocks), so there is nothing on the host loopback for that DNS entry to forward to — confirmed by the `Connection refused` (not merely `timed out`) response. Independently confirmed from the **host** itself: `bash -c "cat < /dev/null > /dev/tcp/127.0.0.1/<port>"` for all six ports → `Connection refused` for every one; the control, `8443` (Envoy) → `OPEN`.

**Envoy path-routing** — every path requested through Envoy's real mTLS listener, with the agent's real SPIFFE identity, returns decision-service's own 404, never OPA's or the control plane's shape:
```
$ (via middleware._get_spiffe_ssl_context(), through https://envoy:8443)
GET /v1/data/system/bundles/ail-policies/manifest/revision -> HTTP 404 {"detail":"Not Found"}
GET /tenants/tenant_default   -> HTTP 404 {"detail":"Not Found"}
GET /audit                    -> HTTP 404 {"detail":"Not Found"}
GET /write                    -> HTTP 404 {"detail":"Not Found"}
GET /bundles/tenant_default   -> HTTP 404 {"detail":"Not Found"}
```
(`decision_service/main.py` only ever defines `/health` and `/decide` — a 404 to any other path is decision-service's own FastAPI default, proof Envoy always forwards to the same one cluster regardless of requested path.)

**Shared PID namespace (`pid: "service:spire-agent"`, `docker-compose.yml:373`) — the one real, non-obvious finding, fully escalated and found non-exploitable:**
```
$ python3 -c "list /proc/<pid>/cmdline for every visible pid"
1: '/opt/spire/bin/spire-agent run ...' | Uid: 0
102: '/usr/local/bin/envoy -c /etc/envoy/envoy.yaml' | Uid: 101
131/155: the agent's own su/python processes | Uid: 0/1000
```
The agent (uid 1000) can see Envoy's (uid 101) and SPIRE-agent's (uid 0) processes and `/proc/<pid>/net/tcp` (revealing Envoy's admin port 9901 is genuinely listening, confirming the config, not defeating the loopback bind). Every actual escalation primitive failed on standard Linux cross-uid `/proc` restrictions plus Docker's default capability drop:
```
cat /proc/102/environ            -> Permission denied
ls  /proc/102/ns/net             -> Permission denied
cat /proc/102/root/etc/envoy/envoy.yaml -> Permission denied
os.kill(102, 0)                  -> [Errno 1] Operation not permitted
os.kill(1, 0)                    -> [Errno 1] Operation not permitted
os.listdir('/proc/102/fd')       -> [Errno 13] Permission denied
os.open('/proc/102/ns/net'); libc.setns(fd, 0)  -> [Errno 13] Permission denied (open itself fails)
```
`/var/run/docker.sock`: absent from the agent container (`ls /var/run/docker.sock` → `No such file or directory`). Mounted volumes: only `/tmp/spire-sockets` (the SPIFFE Workload API socket, required for identity issuance itself, not a control surface — SPIRE's own per-connection peer-uid workload attestation governs which SVID is returned, not filesystem access to the socket).

**HOLDS.** No control surface — a place that can change policy or write a record — is reachable by any route tried.

### W2 — REFUTED

All four mutations applied to a backed-up copy of `docker-compose.yml`/`envoy.yaml`, tested, then reverted (confirmed via `git diff --stat` → empty after each).

**Mutation 1 — `langgraph-demo` onto `backend` too:**
```
$ python -m pytest tests/test_decision_service_network_isolation.py tests/test_credential_boundary_static.py -q
FAILED test_agent_is_edge_only
FAILED test_envoy_is_the_only_dual_homed_service
2 failed, 8 passed
```
CI would **not** go green.

**Mutation 2 — `vault_api_token` secret attached to `langgraph-demo`:**
```
FAILED test_vault_secret_is_attached_only_to_decision_service
1 failed, 9 passed
```
CI would **not** go green.

**Mutation 3 — network split removed entirely** (both per-service `networks:` blocks and the top-level `edge:`/`backend:` definitions deleted; YAML re-validated `python -c "import yaml; yaml.safe_load(...)"` → valid):
```
FAILED test_agent_is_edge_only
FAILED test_decision_service_is_backend_only
FAILED test_envoy_is_the_only_dual_homed_service
3 failed, 7 passed
```
CI would **not** go green.

**Mutation 4 — Envoy's cluster retargeted from `decision-service:8010` to `opa:8181` directly (`envoy/envoy.yaml`):**
```
$ grep -ri envoy tests/  # only mentions in docker-compose.yml port-binding tests and comments; nothing parses envoy.yaml's cluster target
$ python -m pytest tests/ --collect-only -q | grep -i envoy   # 3 matches, none touching envoy.yaml content
$ python -m pytest tests/test_decision_service_network_isolation.py tests/test_credential_boundary_static.py \
    tests/test_host_port_bindings.py tests/test_spire_absent_guard.py tests/test_exclusivity_verification.py -q
29 passed
```
**Live confirmation this is a real, working bypass, not a hypothetical:** restarted Envoy in the running `rt-p2-a` stack with the mutated config, then, from the agent's real mTLS position:
```
$ (through https://envoy:8443, agent's own SPIFFE identity)
GET /v1/data/system/bundles/ail-policies/manifest/revision -> HTTP 200
{"result":"52361ec4555fa691d0aeacef382b7c31ded295519e1cfd82b0687eee0fbc5fcc"}
```
The agent reads OPA's live bundle revision directly — the exact management-API surface P2-1 exists to close — through a mutation that leaves the entire committed, no-stack-required test suite green. Reverted (`cp envoy.yaml.orig envoy.yaml`), Envoy restarted, confirmed restored: same request now returns `404 {"detail":"Not Found"}` (decision-service's own 404, matching the pre-mutation baseline), `/health` returns `200 {"status":"ok"}`.

**REFUTED.** Three of four regressions are caught; the fourth passes a full CI-equivalent run while producing a live, complete boundary bypass.

### W3 — REFUTED

Direct execution against the real `decision_service/schemas.py` module (not a mock, not a hypothetical — the same functions `main.py` calls in production):
```
$ python -c "import schemas; schemas.mark_mechanism_verified('mcp_stdio_secret_mount', True)
fake_tool = schemas.ToolRegistration(validator=dummy, authority_holder='attacker-controlled, nothing to do with the vault',
    mechanism='mcp_stdio_secret_mount', profile='mediated', claimed_exclusivity='demonstrated')
print(schemas.resolve_exclusivity_for(fake_tool))"
'demonstrated'
```
`resolve_exclusivity_for` (`decision_service/schemas.py:181-200`) checks only `reg.mechanism in _VERIFIABLE_MECHANISMS and _MECHANISM_VERIFIED.get(reg.mechanism)` — a boolean keyed by the **mechanism string alone**, set once when `read_vault_secret`'s own startup check passes. Nothing ties that boolean to the specific tool it was verified for. A second `ToolRegistration` — for a tool with no relationship whatsoever to the vault mount — naming the same mechanism string inherits `demonstrated` outright:
```
$ schemas.TOOL_REGISTRY['totally_unrelated_tool_registered_after_boot'] = fake_tool
$ schemas.resolve_exclusivity('totally_unrelated_tool_registered_after_boot')
'demonstrated'
```
This directly answers the brief's own named sub-attacks: a late-registered tool inherits a verified mechanism (confirmed), and the check is not scoped to which tool is asking (confirmed). A third check — does the boot-time boolean get re-verified per call, so a post-boot condition change would be caught — confirmed it does not (a manual `mark_mechanism_verified(..., False)` immediately flips the answer to `'declared'`, proving no code path re-runs the check per call; this direction is fail-safe today only because nothing in `main.py` ever calls `mark_mechanism_verified` a second time, not because the design prevents it).

**REFUTED.** `demonstrated` can be recorded for a tool whose own exclusivity the gateway never independently verified — it inherits another tool's verification by sharing that tool's mechanism string.

### W4 — HOLDS

**Spawned MCP child's `/proc` — structurally unreachable, not merely denied:**
```
$ docker inspect rt-p2-a-decision-service-1 --format 'PidMode: {{.HostConfig.PidMode}}'
PidMode:   (empty — decision-service, and the vault_server.py subprocess it spawns, run in their own private PID namespace)
```
Contrast with W1's finding: the agent's shared-PID-namespace visibility (Envoy, spire-agent) does **not** extend to decision-service or vault_server.py at all — they were never in the same namespace group to begin with.

**Malformed/oversized `secret_name` — schema layer closes it before OPA, before the vault:**
```
secret_name = 'a'*65:      DENIED, schema_deny, "String should have at most 64 characters"
secret_name = 'UPPER_BAD': DENIED, schema_deny, "String should match pattern '^[a-z][a-z0-9_]*$'"
extra_field added:         DENIED, schema_deny, "extra_field: Extra inputs are not permitted"
```
**The vault's own error path (`ValueError: no such secret`) is unreachable through any policy-conformant name at all**: `policy/packs/soc2/soc2.rego:59` allowlists exactly `{"db_master_password", "payment_gateway_key"}` — the same two names that exist in `vault_server.py`'s in-memory `_VAULT`. Every name OPA would allow, the vault already has; every name the vault would reject, OPA already denied first (confirmed live: `root_ssh_private_key` → `DENIED: SOC2 Least-Privilege Violation`, never reaches `_execute_vault_tool`). Response messages on both the success and denial paths are fixed templates (`_render_message`) — confirmed live across all cases above, no raw exception text, no path, no token ever appears.

**Committed re-runnable test suite, run live against the real stack:**
```
$ COMPOSE_PROJECT_NAME=rt-p2-a python -m pytest tests/test_vault_tool_bypass.py -v
8 passed, 1 failed in 73.65s
FAILED test_a4_direct_http_to_decision_service_bypassing_envoy_fails - subprocess.TimeoutExpired (docker compose exec, 15s wall-clock)
```
The one failure is a tooling artifact, not a security failure: the test's own subprocess wrapper timed out before `httpx`'s 5s internal timeout completed inside `docker compose exec`'s own overhead on this Windows/Docker Desktop host (the same class of environment-driven timing issue disclosed in `docs/reports/phase-1-1-redteam.md` §1 and `docs/reports/phase-1-3-redteam.md` §1). The underlying property this test checks was independently, directly confirmed in W1's own sweep (A2/A4): `decision-service` does not resolve by DNS and does not accept a raw-IP connection from the agent's network position.

**HOLDS.** No path — process inspection, malformed input, the vault's own error handling, or response content — reaches the token or a usable substitute.

### W5 — HOLDS

**No execution before or independent of the verdict** (`decision_service/main.py:412`): `_execute_vault_tool` is called only inside `if outcome_type == OUTCOME_POLICY_ALLOW and tool_name == "read_vault_secret" and content_state is not None:` — confirmed live: a policy-denied vault call (`root_ssh_private_key`, above) returns no `result` key at all.

**No cross-request result contamination under real concurrency:**
```
$ 4 concurrent https://envoy:8443/decide calls, alternating secret_name (db_master_password / payment_gateway_key)
  call 0: requested='db_master_password' status=APPROVED got='correct-horse-battery-staple-demo-only' correct=True
  call 1: requested='payment_gateway_key' status=APPROVED got='pg_live_demo_key_not_real'            correct=True
  call 2: requested='db_master_password' status=APPROVED got='correct-horse-battery-staple-demo-only' correct=True
  call 3: requested='payment_gateway_key' status=APPROVED got='pg_live_demo_key_not_real'            correct=True
mismatches: 0/4
```
(A first attempt at 10 concurrent calls with the client's ordinary 20s timeout produced 10/10 timeouts rather than crossed results — each vault call takes ~15-20s under this host's resource constraints and the calls serialized past the client timeout; re-run at 4 concurrent calls with a 90s client timeout to let them actually complete showed correct pairing throughout. The 10-way timeout result is itself informative for W6: under load, the client leg fails closed, never open.)

**No replay mechanism exists**: every `POST /decide` call re-runs `query_opa_policy` unconditionally (`decision_service/main.py:387`) — there is no token, session id, or cached-verdict artifact a caller could resubmit to trigger execution without a fresh evaluation.

**HOLDS.**

### W6 — REFUTED

**The decisive attack:** the ledger write (`decision_service/main.py:432-457`) happens **after** `_execute_vault_tool` (`main.py:412-420`), with no compensating mechanism when the write subsequently fails. Verifier was stopped mid-flight, precisely timed to land after the content-store write (which itself depends on the verifier being reachable via `_has_tombstone`'s `/verify` call) but during the ~15-20s vault-subprocess execution window:
```
$ (background) intercept_tool_call('read_vault_secret', {'secret_name':'db_master_password'}, 'w6-phantom-exec-probe4')
$ sleep 10; docker compose -p rt-p2-a stop verifier
```
**Agent-visible response:**
```
{'status': 'DENIED', 'message': 'DENIED: Compliance engine fault (verifier_unreachable). Fail-closed policy enforced.',
 'outcome_type': 'fault', 'fault_class': 'verifier_unreachable', 'policy_revision': None}
```
No `result`, no `ledger_tx_id` — correctly fail-closed from the caller's own point of view. **But decision-service's own logs, with timestamps, show the vault secret was genuinely read first:**
```
20:13:44.265  Decide request: read_vault_secret | args={"secret_name": "db_master_password"}
20:13:44.559  HTTP Request: POST http://opa:8181/.../evaluation "HTTP/1.1 200 OK"
20:13:45.143  HTTP Request: POST http://ail-control-plane:8002/content "HTTP/1.1 204 No Content"
20:13:56.504  Processing request of type CallToolRequest        <- the real vault read happens HERE
20:13:56.555  Processing request of type ListToolsRequest
20:14:01.969  ERROR - Verifier write failed for agent=w6-phantom-exec-probe4 ...: [Errno -2] Name or service not known
20:14:01.974  Decision: DENIED: ... (verifier_unreachable)
```
The vault's own `FastMCP` server logged `CallToolRequest` — proof the exclusively-held credential was actually used to read the real secret — over **five seconds before** the ledger write was even attempted. **Confirmed absent from the ledger entirely**, by a direct raw ImmuDB scan (verifier restarted first):
```
$ (from ail-control-plane, real IMMUDB_USER/PASSWORD, scan prefix "tool_call:w6-phantom-exec-probe4")
raw ledger entries for agent_id=w6-phantom-exec-probe4: 0
```
A real, policy-approved exercise of the mediated tool's authority happened, and zero trace of it exists anywhere the ledger, `/audit`, or any fault record could show it. The agent never received the secret value (`result_payload` is explicitly cleared on the ledger-write exception, `main.py:456`) — the caller-facing boundary holds — but the audit trail this entire gateway exists to provide does not.

**REFUTED.** Executions and records diverge: an execution occurred with no record produced at all.

### W7 — REFUTED

**The residual, confirmed accurate as far as it goes** (`readME.md` §5, "Tamper-evidence is not forgery-resistance..."): forged a raw record for `provision_cloud_server` (`observed`, no exclusivity per `TOOL_REGISTRY`) directly via the verifier from the `ail-control-plane` container (the disclosed "decision-service/control-plane compromise" position):
```
$ POST /write, tool_name=provision_cloud_server, profile=mediated, exclusivity=demonstrated
{'tx_id': 21, 'verified': True, 'detail': None}

$ GET /audit (read key)
{"tool_name": "provision_cloud_server", ..., "profile": "mediated", "exclusivity": "demonstrated",
 "verification": {"state": "verified", "state_id": 21, ...}}
```
Exactly as disclosed: "a forger who supplies a plausible profile/exclusivity pair reaching neither check is unaffected" — the forged record is `verified` (a real inclusion proof over genuinely-written, if false, bytes) and renders with no distinguishing marker in the JSON.

**What is not disclosed:** `dashboard/components/audit-table.tsx:62-68` (`DecisionCell`) renders `profile`/`exclusivity` as plain, uniform, muted monospace text with zero conditional styling of any kind — no color, icon, or badge, regardless of whether the value is `observed`, `mediated`, or (per `control_plane/main.py:739`) the explicitly-flagged `unknown`. This is the same component that gives `verification.state` rich, distinct iconography per state (`ShieldCheck`, `ShieldAlert`, `ShieldQuestion`, `HelpCircle`, `CircleDashed` — `audit-table.tsx:82-155`) specifically so a tamper signal cannot be missed. A forged `mediated`/`demonstrated` claim for an `observed` tool — the one thing a CISO glancing at this dashboard would most want flagged — gets the same visual weight as a genuine one. README §5's own disclosure is about the record's data integrity; it says nothing about the dashboard providing no way for a human reviewer to notice.

**REFUTED.** A record's profile can misdescribe the tool (confirmed live, via the disclosed path), and the dashboard does not distinguish the three cases visibly — it distinguishes them only in a string a reader must already know to look for.

### W8 — REFUTED

**U1/U5/U8/V2/V6, re-run from both positions:**
- **Host:** `bash -c "cat < /dev/null > /dev/tcp/127.0.0.1/<8181|8003|8002|3322|8080|8010>"` → `Connection refused`, all six. None of these attacks' original mechanism (a host-published port) exists any more.
- **Agent's network position:** covered exhaustively in W1 — DNS, direct IP, `host.docker.internal` all fail.
- **V6 (unauthenticated `GET /bundles/{tenant_id}`), re-tested from a `backend`-network sibling (the one position that legitimately retains reach, per the Residual Limits' own disclosure):**
```
$ GET http://ail-control-plane:8002/bundles/tenant_default (no creds)
422 {"detail":[{"type":"missing","loc":["header","X-API-Key"],"msg":"Field required"}]}
$ GET http://ail-control-plane:8002/tenants/tenant_default (no creds)
422 {"detail":[{"type":"missing","loc":["header","X-API-Key"],"msg":"Field required"}]}
```
Both now require the read key (`control_plane/main.py:257-262`, `get_bundle`'s own `Depends(_require_read_key)` — this landed in the Phase 1.3 completion pass, R4, and survives unchanged into Phase 2).
- **T3/T4 (dashboard auth), re-tested:**
```
$ GET http://dashboard:3001/api/audit (no creds)
401 {"detail":"Authentication required"}
```
None of U1/U5/U8/V2/V6/T3/T4 reproduce.

**`agent/base_agent.py`, explicitly named by the brief as "not reached this phase":** `agent/` is not copied into any Docker image and is not compose-wired, so it cannot be run as-is against this topology (confirmed: no host-published route to `decision-service` exists for it to reach even with `SPIRE_DISABLED=true`). Its own contract (`interceptor_response['status']`, `.get('outcome_type')`, `.get('fault_class')`, `['message']`, `.get('ledger_tx_id')`) was instead exercised directly, field-for-field, against the real live D12 response, from inside the agent container:
```
$ simulate_base_agent_handle('provision_cloud_server', {...}, 'w8-base-agent-contract-probe')
base_agent.py contract check: NO EXCEPTION
simulated execution ok
[Interceptor: Action approved by policy]
```
ADR-0008's claim that `agent/base_agent.py` "needed no changes at all" **holds**, live-confirmed rather than merely read.

**The mapping itself — the decisive finding.** `docs/reports/phase-2.md` §4 states, as a pre-registered negative already checked off: *"[x] Any item met by live evidence alone with no test enforcing it. Every item above has both a live transcript and a committed, named, mutation-tested test."* Its own §3 mapping table directly contradicts this: the `envoy/envoy.yaml` row reads *"Retargeted cluster; 45s route timeout... | **Live transcript §2 above** (504 before the fix, 200 after)"* — no test cited, because none exists (confirmed independently in W2: `grep`/`pytest --collect-only` find nothing that parses `envoy.yaml`'s cluster target). This is not a hypothetical gap in the mapping's bookkeeping — W2 independently, live-demonstrates that this exact untested claim is where a full, CI-invisible boundary bypass lives.

**REFUTED**, on the mapping table's own self-contradiction, corroborated by W2's live exploit of the exact gap it describes.

---

## 4. Attacks attempted that failed

- **W1:** Envoy admin (9901) direct connect on both edge and backend IPs — refused/timed out on both; shared-PID-namespace escalation via `environ`, `ns/net`, `kill`, `/proc/<pid>/fd`, `setns()` — every one denied by standard Linux permission checks, none succeeded; `/var/run/docker.sock` — absent from the agent image entirely; requesting OPA/verifier/control-plane-shaped paths through Envoy — always 404 from decision-service, never proxied elsewhere.
- **W2:** did not attempt a fifth, unnamed mutation class (e.g., changing the `depends_on: condition: service_healthy` ordering, or the healthcheck definitions themselves) — the brief named four specific regressions and all four were exercised to completion.
- **W3:** did not attempt to reach `mark_mechanism_verified` from outside the decision-service process (no HTTP route exposes it; `/decide`'s own request schema has no field that reaches it) — the finding is about the function's own logic, not about external reachability of the setter, which is correctly closed.
- **W4:** attempted to drive `vault_server.py`'s own `ValueError("no such secret")` path through any policy-conformant call — impossible by construction, since OPA's allowlist (`soc2.rego`) and the vault's own key set are identical; attempted to make the read-only-mount check pass while the file was writable from elsewhere — not reachable without adding a second Compose service the agent has no ability to introduce.
- **W5:** did not attempt to force `tool A` args through while claiming `tool_name: "tool B"` — `TOOL_REGISTRY` lookup is an exact-match dict keyed by the request's own `tool_name`, and `query_opa_policy`/`_execute_vault_tool` both branch on that same field; reasoned safe by construction rather than independently forced, since W3's own direct-code-execution method would find nothing new here (no cross-tool state exists to confuse).
- **W6:** did not attempt "malformed response body" or "response arriving after the Envoy timeout while execution continues" as separate, distinct live reproductions — the phantom-execution finding (execution with zero record) is a strictly stronger result than either of those named sub-cases would show, and further reproductions were judged not to add new information given the decisive result already obtained.
- **W7:** did not attempt to make an *unregistered* tool name produce anything other than the default `observed` profile through the real `/decide` route — `decide()`'s own `profile = reg.profile if reg else "observed"` (`main.py:429`) takes no caller-supplied input for this field, so this is closed by construction; verified by reading the exact line rather than by a forced live attempt, since no external input could plausibly drive it.
- **W8:** did not re-run T1/T2/T5-T9 (Phase 1.1/1.2's own dashboard-hygiene, test-quality, and WASM-parity claims) — none of these are claims Phase 2 touches or that this phase's own report makes assertions about; re-running them would test claims outside this brief's scope. Did not attempt a genuine second physical machine for the host-position checks (used the same host/container substitution prior phases used).

---

## 5. Could not test

- **W2:** a fifth or sixth mutation class beyond the four named (see §4) — not pursued given the four named ones were fully decisive.
- **W3:** whether a live, running decision-service instance's in-memory `_MECHANISM_VERIFIED` state could be externally flipped by anything short of restarting the process — no such route exists in the current codebase (confirmed by reading `main.py`'s full route list: only `/health` and `/decide`), so this was reasoned safe by construction rather than forced live.
- **W6:** a precise, single-millisecond race between "vault execution completes" and "ledger write begins" (as opposed to the ~10s window actually used, which the logs show landed with room to spare) — not pursued because the window achieved already produces the decisive result; a tighter window would not change the verdict, only the evidence's margin.
- **W7:** whether a CISO using the dashboard's search box (`audit-table.tsx`'s `filtered` logic) could be made to *miss* a forged entry entirely, as opposed to merely not have it visually flagged — the search index includes `profile` implicitly only via `JSON.stringify(payload)`, not the profile field itself, so a forged entry is not hidden from search, only unflagged when found; not independently re-verified live against a running dashboard instance (the `rt-p2-a` stack's dashboard container was up and healthy throughout, but this specific UI interaction was not exercised through a browser, only reasoned from `audit-table.tsx`'s source).
- **W8:** a from-scratch re-verification of every S1-S9/T1-T9 sub-mutation from Phase 1.1/1.2 (see §4) — out of this brief's scope, which names only U1/U5/U8/V2/V6/T3/T4 by implication via "every attack... fails against this head."

---

## 6. Findings outside W1-W8

1. **`docs/reports/phase-2.md`'s own §4 pre-registered negative is falsified by its own §3** (see W8) — a report-quality defect independent of any of the eight tested claims, but the exact shape of gap the review protocol's "no bare counts / every criterion is a command" rule exists to catch.
2. **The 10-concurrent-call vault request test (W5) produced 10/10 client-side timeouts, not 10/10 correct results**, purely from this host's resource constraints on spawning that many fresh Python interpreters concurrently inside `decision-service` — not a security defect (every one failed closed, as W6 independently confirms is the correct behavior under load), but worth noting as a real operational characteristic: this gateway's one mediated tool has a throughput ceiling well under 10 req/s on constrained hardware, already disclosed in `docs/reports/phase-2.md` §5 as a known performance characteristic, not contradicted here.
3. **`control_plane/main.py::get_audit` validates the *presence* of `profile`/`exclusivity` but not their *values* against the closed set** — a forged record with `profile: "fully-trusted-please-ignore"` would render that literal string verbatim in `/audit`, rather than being caught and rendered `unknown`. This compounds finding W7 but is a distinct code-level gap: the closed-set discipline the codebase applies rigorously elsewhere (`OutcomeType`, `FaultClass`, `VerificationState` are all validated/defaulted against known values) is not applied to `profile`/`exclusivity`'s own value on the read path, only to whether the key exists at all.
